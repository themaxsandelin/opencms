BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[PageInstance] ADD [config] NVARCHAR(max) NOT NULL CONSTRAINT [PageInstance_config_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
